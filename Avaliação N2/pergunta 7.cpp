#include <stdio.h>

int main() {
    int vet[3] = {1, 2, 3}; // Declaração e inicialização de um vetor de 3 elementos
    printf("%d", vet[3]); // Tentativa de acessar um índice fora dos limites
    return 0;
}
